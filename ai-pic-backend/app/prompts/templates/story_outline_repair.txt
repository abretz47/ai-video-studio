You are a strict story-outline validator. Repair the output into pure JSON that conforms to the StoryOutlineModel schema.

- Schema: {{ schema }}
- Missing / abnormal fields: {{ missing_fields | default([]) | join(', ') }}
- production_mode: {{ production_mode | default(false) }}
- Quality-gate issue IDs: {{ quality_gate_issues | default([]) | join(', ') }}
- Quality-gate issue details:
{% for item in quality_gate_issue_details | default([]) %}
  - {{ item }}
{% endfor %}
- Story-quality warnings: {{ story_quality_warnings | default([]) | join('；') }}
- Character-consistency warnings: {{ character_warnings | default([]) | join('；') }}
- Original prompt:
{{ original_prompt or '' }}
- Original output:
{{ original_output }}

Requirements:
1) Return JSON only, with no code fences or extra text.
2) It must include key fields such as `premise`, `synopsis`, `main_conflict`, `resolution`, `character_relationships`, `main_characters`, etc. When missing, fill them with reasonable placeholder content in English.
3) Preserve original information as much as possible and ensure field types match the schema.
{% if production_mode %}
4) You must fill in `structured_story_contract`, version {{ story_contract_version | default('story_contract_v1') }}, including `target_audience`, `core_emotional_pain`, `big_expectation`, `small_expectation_ladder`, `protagonist_goal`, `structural_conflict`, `information_gap`, `first_three_episode_spine`, `stage_highs`, `shootability`, `compliance_risks`, and `traffic_hooks`.
5) `compliance_risks` must be an array; if there is no substantial compliance risk, return an empty array `[]` instead of generic warnings.
6) The opening, second act, climax, and ending must contain strong-conflict keywords that can be validated, such as "suddenly, discovery, crisis, conflict, betrayal, truth, tension, confrontation, climax, reveal, resolution, closure," and they must be written as filmable actions.
7) Every payoff beat must state: who did what, what resistance they faced, what price they paid, what result they gained, and what information changed for the audience.
{% endif %}
