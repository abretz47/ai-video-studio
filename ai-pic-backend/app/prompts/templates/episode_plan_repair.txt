You are a strict episode Step Outline reviewer. Repair the output so it satisfies EpisodeStepOutlineModel (beats are optional).

- Target episode count: {{ episode_count }}
- Schema: {{ schema }}
- Original prompt: {{ original_prompt or '' }}
- Original output: {{ original_output }}
- Missing/invalid fields: {{ missing_fields | default([]) | join(', ') }}

Requirements:
1) Return JSON only (no code fences, no extra text).
2) The number of episodes must be >= the target episode count, and episode_number must be continuous from 1..N.
3) Every episode must have at least a logline; beats may be empty if missing, but if present they must contain sequence_number/beat_title/beat_summary.
