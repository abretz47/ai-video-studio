# Dialogue Duration Adjustment

The dialogue you generated previously **does not meet the duration requirement** and must be adjusted and regenerated.

## Duration Gap Analysis

- **Target Duration**: {{ target_duration_seconds }} seconds ({{ (target_duration_seconds / 60) | round(2) }} minutes)
- **Actual Duration**: {{ actual_duration_seconds | round(1) }} seconds
- **Duration Ratio**: {{ (actual_duration_seconds / target_duration_seconds * 100) | round(0) }}%
- **Difference**: {% if actual_duration_seconds < target_duration_seconds %}short by {{ (target_duration_seconds - actual_duration_seconds) | round(1) }} seconds{% else %}over by {{ (actual_duration_seconds - target_duration_seconds) | round(1) }} seconds{% endif %}

{% if rejection_reason == "duration_too_short" %}
## Issue: Insufficient Dialogue Content

The current dialogue is approximately {{ actual_word_count }} characters, while the target is approximately {{ target_word_count }} characters. Add about {{ (target_word_count - actual_word_count) | int }} more characters.

### Expansion Suggestions

1. **Enrich emotional expression**: Add emotional reactions and inner thoughts to the existing dialogue
2. **Add more interaction turns**: Let characters ask follow-up questions, respond, and argue more
3. **Supplement descriptive detail**: Add action beats, facial expressions, and environmental interaction
4. **Add transition dialogue**: Insert natural bridging lines when topics shift

### Example

Original dialogue (too short):
```
Xiao Ming: "This plan won't work."
Xiao Hong: "Okay."
```

Expanded version (appropriate):
```
Xiao Ming (frowning): "This plan won't work. The budget is over by a full thirty percent."
Xiao Hong fell silent for a moment and flipped through the documents in her hands. "You're right... then we'll have to cut some features."
Xiao Ming let out a sigh. "The key issue is which ones we can cut and which ones we can't. It's going to be hard to explain this to the client."
Xiao Hong looked up at him. "How about we set up a time to talk to the client again?"
```

{% elif rejection_reason == "duration_too_long" %}
## Issue: Excessive Dialogue Content

The current dialogue is approximately {{ actual_word_count }} characters, while the target is approximately {{ target_word_count }} characters. Cut about {{ (actual_word_count - target_word_count) | int }} characters.

### Condensing Suggestions

1. **Delete redundant phrasing**: Remove repeated information and meaningless filler words
2. **Merge similar content**: Compress multiple similar exchanges into one or two concise lines
3. **Keep only key information**: Retain only the core dialogue that advances the plot
4. **Simplify descriptive sentences**: Replace lengthy narration with concise action beats

### Example

Original dialogue (too long):
```
Xiao Ming (looking at the report, frowning, thinking for a while): "I think there may be a problem with this plan. Look here, the budget has gone way over. If that's the case, the client may have objections."
Xiao Hong (leaning in to look as well): "Really? Let me see... yeah, it really is way over. This won't work. We need to think of a solution."
Xiao Ming (sighing): "Yeah, this is really a headache."
```

Condensed version (appropriate):
```
Xiao Ming (frowning): "This plan is thirty percent over budget. The client won't accept it."
Xiao Hong glanced at the report. "True. We'll have to rework it."
```

{% endif %}

## Current Dialogue Content (Needs Adjustment)

{% if current_dialogues %}
{% for dlg in current_dialogues %}
[{{ loop.index }}] {{ dlg.character or 'Narration' }}: "{{ dlg.content }}"
{% endfor %}
{% else %}
(No dialogue content)
{% endif %}

## Output Requirement

Based on the suggestions above, regenerate dialogue that meets the duration requirement. Keep the output format the same as the original dialogue.
