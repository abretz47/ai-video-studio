Based on the [story information], generate an executable serialized-writing outline (`plan`) for a Zhihu-style long-form story.

[Story information]
{{ story | tojson }}

[Target constraints]
- Target word count: {{ target_words }}
- Number of chapters: {{ chapter_count }}

Please output **strict JSON** (no code fences, no extra explanation) in the following structure:
{
  "question_title": "Zhihu question title (1 line)",
  "question_detail": "Additional question description (use \n for line breaks, 2-6 lines, written like a Zhihu question; do not include real newline characters inside the string)",
  "narrator_profile": "Answerer's self-introduction / position (use \n for line breaks, 1-4 lines, for example stakeholder / eyewitness / observer; do not include real newline characters inside the string)",
  "running_summary_seed": "Initial running summary used to maintain continuity (may be an empty string)",
  "chapters": [
    {
      "chapter_number": 1,
      "title": "Update title (short phrase)",
      "chapter_goal": "Goal / intended progression of this chapter (1-2 sentences)",
      "cliffhanger_hint": "End-of-chapter cliffhanger / suspense hint (1-2 sentences)"
    }
  ]
}

Rules:
- `chapters.length` must == {{ chapter_count }}
- `chapter_number` values inside `chapters` must start from 1 and increase in order
- Every chapter must include `cliffhanger_hint` (the final chapter may use a "closure + lingering aftertaste" style cliffhanger)
- `chapter_goal` must be specific and executable, not vague
- Output JSON only
- Real newline characters are forbidden inside JSON strings. If you need a line break, write it as \n
